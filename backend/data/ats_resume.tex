\documentclass[10pt,a4paper]{article}

% Packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[margin=0.5in]{geometry}
\usepackage{enumitem}
\usepackage{hyperref}

% Hyperref setup
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    urlcolor=blue,
    pdftitle={CV - CareerMatch},
    pdfauthor={CareerMatch}
}

% Custom settings
\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setlist[itemize]{left=0pt, label={}, nosep, itemsep=0pt, topsep=2pt}

% Section formatting
\newcommand{\cvsection}[1]{
    \vspace{6pt}
    \textbf{\MakeUppercase{#1}}
    \vspace{2pt}
    \hrule
    \vspace{4pt}
}

% Experience entry
\newcommand{\cventry}[4]{
    \textbf{#1} \hfill \textit{#2}\\
    \textit{#3} \hfill #4
    \vspace{1pt}
}

\begin{document}

% Header
\begin{center}
    {\LARGE \textbf{{{ name }}}}\\
    \vspace{2pt}
    {{ location }} | \href{mailto:{{ email }}}{{{ email }}} | {{ phone }}
\end{center}

\vspace{4pt}

% Profile
\cvsection{Profil}
{{ summary }}

% Education
\cvsection{Formation}
{% for edu in education %}
\cventry{{ edu.degree }}{}{{{ edu.institution }}}{{{ edu.details }}}
{% if not loop.last %}\vspace{2pt}{% endif %}
{% endfor %}

% Professional Experience
\cvsection{Expériences Professionnelles}
{% for exp in experience %}
\cventry{{ exp.position }}{{{ exp.start_date }} - {{ exp.end_date }}}{{{ exp.company }}}{{{ exp.location }}}
\begin{itemize}
{% for item in exp.description %}
    \item {{ item }}
{% endfor %}
\end{itemize}
{% if not loop.last %}\vspace{3pt}{% endif %}
{% endfor %}

% Technical Skills
\cvsection{Compétences Techniques}
\noindent
{% for skill in skills %}
{{ skill }}{% if not loop.last %} | {% endif %}
{% endfor %}

% Languages
\cvsection{Langues}
\noindent
{% for lang in languages %}
{{ lang }}{% if not loop.last %} | {% endif %}
{% endfor %}

% Certifications
\cvsection{Certificats}
\begin{itemize}
{% for cert in certifications %}
    \item {{ cert }}
{% endfor %}
\end{itemize}

\end{document}